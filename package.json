{
  "name": "cypress_novo_proj",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "cypress open --browser chrome",
    "cy:run:chrome": "cypress run --browser chrome",
    "cypress:open:dev": "cypress open --config-file config-dev.js --browser chrome",
    "cypress:run:dev": "cypress run --config-file config-dev.js --browser chrome",
    "cypress:open:qa": "cypress open --config-file config-qa.js --browser chrome",
    "cypress:run:qa": "cypress run --config-file config-qa.js --browser chrome"
  },
  "keywords": [],
  "type": "commonjs",
  "devDependencies": {
    "cypress": "^16.0.0",
    "cypress-mochawesome-reporter": "^5.0.0",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^18.0.1",
    "mocha-junit-reporter": "^2.2.1"
  },
  "allowScripts": {
    "cypress@16.0.0": true
  }
}
